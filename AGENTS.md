# このリポジトリでの作業方針

## 目的

このアプリは、転職活動で開発・評価・改善の過程を説明するためのポートフォリオである。自治体の人事・給与制度に関する架空文書を検索し、根拠付きで回答する既存のRAGを改善する。新規アプリを増やす前に、このリポジトリの完成度と説明可能性を高める。

## 現状と優先順位

- 既存の構成は Python、LangChain、Gemini、Chroma、Streamlit。Docker Compose と Cloud Run の構成、および検索・回答品質の評価スクリプトがある。変更前に実際のコードと動作を確認する。
- 次に優先するのは、再現可能な CI、難しい質問を含む評価セット、失敗原因の分析、同一評価セットでの改善前後比較である。
- SQL の実務経験とのつながりを示すため、RAGログを PostgreSQL に保存し、根拠を取得できなかった質問などを SQL で集計できるようにする。Text-to-SQL は後続の候補であり、実装済みとして説明しない。
- クラウド費用、API呼び出し費用、公開デモの動作状況は、推測や過去の記述だけで確定せず、その時点の証拠で確認する。

## 実装と評価

- 変更は一つの検証可能な目的に絞る。既存の検索・回答・ログの動作を保ち、必要なテストと実行手順を更新する。
- 評価質問には、複数文書の参照、文書に答えがない質問、言い換え・略語・誤字、似ているが答えの異なる質問を含める。答えが文書にない質問を検索失敗として集計しないよう、評価対象と正解条件を明示する。
- 検索失敗、回答生成の失敗、回答分類の失敗、文書に根拠がないケースを区別して記録する。改善前後は同じ質問セット・同じ指標で比較し、件数と個別の失敗例を残す。
- Recallなどの指標は、評価セットの件数・内容・測定条件と一緒に示す。目標値を達成するために質問を調整しない。効かなかった変更も結果として記録する。
- 本番の人事・給与データや個人情報を持ち込まない。APIキー、`.env`、ログ内の機密情報をコミットしない。

## 対外的な説明

- READMEと職務経歴書に書く実装内容・数値・クラウド運用状況は、検証済みの事実に合わせる。計画中の機能や未計測の効果を実績として記載しない。
- 技術選定、失敗原因、改善の効果と限界を、面接でコードや評価結果を示しながら説明できる形にする。

## 繰り返す作業とskills

- ユーザーから同種の指示が繰り返され、手順や判断基準を再利用できる場合は、skillとしてまとめることを提案する。提案時は、対象となる作業、再利用する手順、skill化による利点を簡潔に示す。
- 単発の作業や、このリポジトリ固有の短い方針はAGENTS.mdに留める。skillの作成・変更は、ユーザーと対象範囲を決めてから行う。

この方針は、転職ロードマップのうち本リポジトリに関係する事項を要約したもの。ロードマップ自体は作業の参考資料であり、個々の提案は実装前に現状と費用を確認して判断する。
